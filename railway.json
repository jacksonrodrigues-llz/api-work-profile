{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "java -Dserver.port=$PORT -Dspring.profiles.active=railway -jar app.jar",
    "healthcheckPath": "/actuator/health",
    "healthcheckTimeout": 300
  }
}
